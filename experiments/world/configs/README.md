# Configs

Store small, reproducible config files for world-model smoke tests and ablations.

Expected first configs:

- Part 1 JEPA latent smoke run.
- Part 1 variance/covariance ablation.
- Part 2 conditional flow decoder smoke run.
- Part 2 decoder ablation with frozen vs semi-frozen world-model encoder.

Prefer short config names that include the experiment ID once numbered.
